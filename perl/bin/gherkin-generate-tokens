#!/usr/bin/env perl

use strict;
use warnings;
use lib 'lib';

use Gherkin::Parser;
use Gherkin::TokenFormatterBuilder;

my @file_list = @ARGV;

my $parser = Gherkin::Parser->new( Gherkin::TokenFormatterBuilder->new() );

print join "\n", @{ $parser->parse($_) } for @file_list;
print "\n";
